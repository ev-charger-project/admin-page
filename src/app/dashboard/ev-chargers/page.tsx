"use client";
import React, { FC } from "react";
import EVChargerList from "@/container/ev-charger/EVChargerList";

const EVCharger: FC = () => {
    return <EVChargerList />;
};

export default EVCharger;
